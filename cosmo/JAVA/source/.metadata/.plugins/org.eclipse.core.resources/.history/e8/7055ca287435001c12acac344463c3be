package sec06.exam01_array_bylist;

public class ArrayCreatByValueListExample2 {
	public static void main(String[] args) {
//		int[] scores = {83, 90, 87}; 이렇게 순서를 나타내는 객체는 한줄에 초기화를 시켜야함
		
//		int[] scores; 
//		scores = {83, 90, 87}; // 이렇겐 안됀다 컴파일 에러남
		
		int[] scores;
		scores = new int[] {83, 90, 87}; // 객체를 new로 잡는 거라면 가능
		
		
		int sum = 0;
		for(int i=0; i<scores.length; i++) {
			sum += scores[i];
			//일일히 하나하나 지정 안해도 이걸로 쉽게 전개가 됨
		}
		System.out.println("총합 : " + sum);
	}
	// 배열 매개변ㅜ의 값들 합계를 리턴해준다
	public static int add(int[] scores) {
		 int sum = 0;
		 for(int i=0; i<scores.length; i++) {
			 sum += scores[i];
		 }
		 return sum;
	}

}

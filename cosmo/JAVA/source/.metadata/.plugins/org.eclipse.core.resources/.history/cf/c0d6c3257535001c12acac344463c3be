package sec06.exam01_array_bylist;

public class ArrayCreatByValueListExample2 {
	public static void main(String[] args) {

		
//		int[] scores; 
//		scores = {83, 90, 87}; // 이렇겐 안됀다 컴파일 에러남
		
		int[] scores;
		scores = new int[] {83, 90, 87}; // 객체를 new로 잡는 거라면 가능
		
		
		int sum = 0;		
		sum = add(scores);
		System.out.println("총합 : " + sum);
		
		
		//리턴된 총 합을 sum2에 대입되어짐
		int sum2 = add(new int[] {70, 90, 87});
		System.out.println("총합 : " + sum);
		System.out.println("");
	
	}
	// 배열 매개변수의 값들 합계를 리턴해준다. 미리 여기서 계산을 전부 다 해서 위로 리턴시켜주니 굳이 다시 계싼식을 쓸 필요가 없다
	// int[] scores = new int[] {83, 90, 87} 
	public static int add(int[] scores) {
		 int sum = 0;
		 for(int i=0; i<scores.length; i++) {
			 sum += scores[i];
		 }
		 return sum;
	}

}
